services:
  file-watcher:
    build: .
    container_name: inbound
    restart: unless-stopped
    volumes:
      - ${WATCH_FOLDER:-./watch}:/watch
      - ${STATE_FILE:-./data/state.json}:/state.json
      - ${LOG_FILE:-./data/watcher.log}:/watcher.log
    env_file:
      - .env
    environment:
      - WATCH_FOLDER=/watch
      - STATE_FILE=/state.json
      - LOG_FILE=/watcher.log
